import { CredentialErrors } from '@cloud-code/contracts/modules/credential/errors';
import { defineErrors } from '@/shared/errors/defineErrors';

export const CredentialError = defineErrors(CredentialErrors);
